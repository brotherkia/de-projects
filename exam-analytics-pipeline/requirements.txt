# Only needed to run dags/ or scripts/ outside Docker; the docker-compose
# setup brings its own Airflow.
apache-airflow==2.9.3

# NOTE on pandas versions -- two environments, deliberately different:
#   - Inside the Airflow container, Airflow 2.9.3's own constraints pin
#     pandas==2.1.4, so docker-compose.yml pins that exactly. Letting pip
#     resolve pandas freely there installs pandas 3 and breaks Airflow's deps.
#   - Outside Docker, the scripts are verified on pandas 3.0.5 (2.1.4 has no
#     wheel for Python 3.13+).
# The code is written to work on both -- see _df_from_xcom() in the DAG.
pandas
psycopg2-binary
kafka-python
